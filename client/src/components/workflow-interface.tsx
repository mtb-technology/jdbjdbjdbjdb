// This component has been refactored and split into smaller, manageable components
// Import the new workflow manager instead
import WorkflowManager from "./workflow/WorkflowManager";

export default WorkflowManager;